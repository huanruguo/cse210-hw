namespace ScriptureMemory
{
    public enum Difficulty
    {
        Easy = 0,      // Keep only the first letter
        Normal = 1,    // Hide the entire word
        Hard = 2       // Fixed length underscores
    }
}
